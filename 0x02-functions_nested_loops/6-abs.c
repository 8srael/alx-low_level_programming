#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @n : number that you want to know his absolute value
 *
 * Return: absolute value of n.
*/

int _abs(int n)
{
	return ((n > 0) ? n : -n);
}
